# Setup

1. Clone repository
2. Run `docker-compose up` (or `docker compose up` depending on your installation).
3. After waiting for all containers to download and start, check if [Elasticsearch](http://localhost:9200) and [Kibana](http://localhost:5601) are up.
